<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AyosApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="android:colorBackground">@color/soft_blue</item>
        <item name="android:textColorHint">#D3CBCB</item>
    </style>
    <style name="Theme.AyosApp" parent="Base.Theme.AyosApp" />

    <style name="floating1" parent="Widget.Design.FloatingActionButton">
        <item name="android:foreground">
            @drawable/floating_edit_button
        </item>

    </style>

</resources>